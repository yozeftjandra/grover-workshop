# Core dependency of the grover_workshop support package.
# MCMTGate (used in oracles.py) was introduced in qiskit 1.3 -- pyproject.toml's
# previous ">=1.0" floor was too low and would ImportError on 1.0-1.2.x.
qiskit>=1.3

# Needed by the workshop notebooks (circuit plotting, matplotlib drawer backend).
matplotlib
pylatexenc
